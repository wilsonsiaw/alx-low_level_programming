#include <stdio.h>

/**
 * print string - Print string using puts()
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	char str[] = "Programming is like building a multilingual puzzle";

	puts(str);
	return(0);
}
